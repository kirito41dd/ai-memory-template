## 项目记忆机制

本项目使用 `.ai/memory/` 管理长期记忆，采用**渐进式披露**：不要预读所有记忆文件，按需加载。

### 启动流程

新会话或任务开始时：
1. **只读** `.ai/memory/index.md`（约 150 行，含触发条件和标签索引）
2. 根据当前任务关键词，判断需要读取哪些具体记忆文件
3. 只读相关文件，不要一次性 `cat` 整个 `.ai/memory/` 目录
4. 读取前先看文件头部 `<!-- SUMMARY -->` 块判断相关性

### 按需加载示例

- 用户问"为什么这么做" / 做技术选型 → 读 `decisions.md`
- 用户提新需求 / 讨论功能范围 → 读 `requirements.md`
- 调试 bug / 修改敏感模块前 → 读 `gotchas.md`
- 写新代码 / 用户纠正风格 → 读 `conventions.md`
- 遇到陌生术语 → 读 `glossary.md`

### 写入流程

识别到值得记忆的内容时（决策/需求/坑/约定/术语），**先询问用户**，确认后：

1. 查 `index.md` 找到对应文件
2. 追加到文件顶部（倒序时间线）
3. **同步更新 `index.md`**：
   - 在"最近热点"区块追加新条目（保持最多 10 条，超出则移除列表中最旧的条目）
   - 更新"标签索引"（提取关键词）
   - 如涉及新模块，更新"快速索引"
4. **同步更新记忆文件的 SUMMARY 块**：
   - 更新"条目数"（+1）
   - 更新"最近更新"日期
   - 更新"高频标签"（如有新标签）
5. 检查文件行数，超过 500 行时归档最旧的 100 行到 `archive/<year>/<filename>`（优先归档标题或状态字段包含 `[DEPRECATED]` 的条目）

**触发话术**：
> 这个 [决策/需求/坑] 值得记入项目记忆吗？我会追加到 `.ai/memory/xxx.md`。

### 写入格式

每条记忆必须包含：
- 日期（YYYY-MM-DD，作为二级标题）
- 一句话标题
- 内容（为什么、是什么、影响范围）
- 关联文件/commit（可选）

### 维护边界

- 不主动修改本文件（CLAUDE.md）
- 不删除历史记忆，过时标记 `[DEPRECATED]`
- 避免跨文件重复，用完整锚点引用（如 `见 decisions.md#2026-04-29-选择-rust-作为主语言`）
- 写入前先 grep 现有内容，重复则更新而非新增
- `glossary.md` 写入后需按字母顺序重排序
