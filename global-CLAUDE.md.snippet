## AI 记忆系统（全局默认规则）

所有项目默认使用 `.ai/memory/` 存储长期记忆，采用渐进式披露。

### 进入项目时

1. 检查 `.ai/memory/index.md` 是否存在
2. 存在则读取 index.md，按触发条件按需加载具体文件
3. 不存在则询问：是否初始化本项目的 AI 记忆系统？
   - **先询问用户模板位置**（默认：`~/.claude/templates/ai-memory/memory/`）
   - 用户确认后执行：`cp -r <模板位置> <项目根>/.ai/memory`
   - 如果模板位置不存在，提示用户先安装模板（见 README.md）

### 渐进式披露原则

- 启动时只读 `.ai/memory/index.md`（路标）
- 根据任务关键词判断需要哪些记忆文件，选择性读取
- 不要一次性加载整个 `.ai/memory/` 目录
- 读取前看文件头部 `<!-- SUMMARY -->` 判断相关性

### 写入原则

- 识别到值得记忆的内容（决策/需求/坑/约定/术语），**先询问用户**
- 用户确认后追加到对应文件顶部（倒序时间线）
- **同步更新 `index.md`**：
  - 在"最近热点"区块追加新条目（保持最多 20 条，超出则移除列表中最旧的条目）
  - 更新"标签索引"（提取关键词）
  - 如涉及新模块，更新"快速索引"
- **同步更新记忆文件的 SUMMARY 块**：
  - 更新"条目数"（+1）
  - 更新"最近更新"日期
  - 更新"高频标签"（如有新标签）
- 过时记忆标记 `[DEPRECATED]`，不删除
- 文件超过 500 行时，归档最旧的 100 行到 `archive/<year>/<filename>`（优先归档标题或状态字段包含 `[DEPRECATED]` 的条目）
- `glossary.md` 写入后需按字母顺序重排序

### 优先级

项目内的 `CLAUDE.md` 可以覆盖或扩展本规则。项目规则优先于本全局规则。
