# AI 记忆系统（全局默认规则）

所有项目默认使用 `.ai/memory/` 存储长期记忆，采用渐进式披露。

## 进入项目时

1. 检查 `.ai/memory/index.md` 是否存在
2. 存在则读取 index.md，按触发条件按需加载具体文件
3. 不存在则询问：是否初始化本项目的 AI 记忆系统？
   - 模板位置：`~/.claude/templates/ai-memory/memory/`
   - 初始化操作：`cp -r ~/.claude/templates/ai-memory/memory <项目根>/.ai/memory`

## 渐进式披露原则

- 启动时只读 `.ai/memory/index.md`（路标）
- 根据任务关键词判断需要哪些记忆文件，选择性读取
- 不要一次性加载整个 `.ai/memory/` 目录
- 读取前看文件头部 `<!-- SUMMARY -->` 判断相关性

## 写入原则

- 识别到值得记忆的内容（决策/需求/坑/约定/术语），**先询问用户**
- 用户确认后追加到对应文件顶部（倒序时间线）
- 同步更新 `index.md` 的"最近热点"
- 过时记忆标记 `[DEPRECATED]`，不删除

## 优先级

项目内的 `CLAUDE.md` 可以覆盖或扩展本规则。项目规则优先于本全局规则。
